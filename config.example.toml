# 复制为 config.toml 并填入自己的凭据
[server]
host = "your.allstar.node"
port = 4569

[auth]
username = "YOURCALL"
secret   = "your-secret-here"

[caller]
callerid = "YOURCALL"

[call]
node = "1999"

[audio]
codec = "ulaw"

[activity]
threshold = 50.0   # RMS 阈值，超过即判定有人上话
hang_ms = 500      # 静音多久判定上话结束
